<?php

/* $Id$ */

!defined('DIR_SYSTEM') && exit();

/*
class PinqForeignResource extends Resource implements Package {
	
	const RESPONSE_AUTO = 1, // auto detect the response type from headers
	      RESPONSE_TEXT = 2,
	      RESPONSE_JSON = 4,
	      RESPONSE_XML = 8,
	      RESPONSE_HTML = 16,
	      RESPONSE_CSV = 32,
	      RESPONSE_CUSTOM = 64;
	
	public $protocol = 'http',
	       $url,
	       $port = 80,
	       $user_name,
	       $password,
	       $response_type = self::RESPONSE_AUTO,
	       $response_handler;
	
	public function beforeAction($method) {
		
	}
}
*/