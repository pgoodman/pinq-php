<?php

/* $Id: */

!defined('DIR_SYSTEM') && exit();

/**
 * For databases, PQL can do some strange things to the queries and so it
 * makes sense to decompile whats been compiled into the queries.
 * @author Peter Goodman
 */
final class QueryDecompiler {
	
	/**
	 * Decompile a query into some record objects.
	 * @internal
	 */
	static public function getRecord(array $data, Dictionary $models) {
		
		
	}
}